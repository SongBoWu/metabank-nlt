export enum LevelType {
  DEPOSIT = "Deposit",
  FOREX = "Foreign Exchange",
  LOAN = "Loan",
}

export enum LevelMode {
  PRACTICE = 1,
  CHALLENGE,
}

export enum LevelStatus {
  STARTED = 1,
  FINISHED,
}